const register = async (options) => {
    try {
        
    } catch (error) {
        throw new Error(error);
    }
}

const login = async (options) => {
    try {

    } catch (error) {
        throw new Error(error);
    }
}

module.exports = {
    register,
    login
}